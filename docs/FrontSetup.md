# 起動方法

# ReactNative 環境構築

# 1. 環境の確認（インストールされているかチェック）
node -v
npm -v

# 2. JavaScriptプロジェクトの作成（不要なファイルがない最小限の構成）
npx create-expo-app AwesomeProject --template blank

# 3. プロジェクトフォルダへの移動
cd AwesomeProject

# 4. 開発サーバーの起動
npm start

# 5. 必要なライブラリのインストール
npx expo install @react-navigation/native @react-navigation/bottom-tabs react-native-screens react-native-safe-area-context

```

```